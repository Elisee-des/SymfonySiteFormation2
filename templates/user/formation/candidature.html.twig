{% extends "base.user.html.twig" %}

{% block titre %}{% endblock %}

{% block body %}

	<div>
		<div class="container">
			<div class="row">
				<div class="col-lg-6 offset-lg-3">
					<div class="section-heading">
						<h2>Je postule a cette formation
						</h2>
					</div>
				</div>
			</div>
			{% form_theme form "bootstrap_4_layout.html.twig" %}
			{{form(form)}}
			</div>
		</div>
	{% endblock %}

	{# {% form_theme form "bootstrap_4_layout.html.twig" %}
		
		{{ form(form) }} #}
